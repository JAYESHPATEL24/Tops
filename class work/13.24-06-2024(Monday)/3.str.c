/*String is a sequence of characters, 
terminated by a null character, 
that can be ordered, indexed, and compared. */


#include<stdio.h>
main()
{
	char a[3];
	printf("Enter a String : ");
	gets(a); 
	
	printf("%s",a);
}

